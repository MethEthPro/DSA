#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main(){


    // search insert position 

    vector<int> myarr={1,2,5,8,13,13,15,17,21,24};

    // its same as lowerbound index for eg for x=13
    // here the lower bound index will be 4,
    // as we have seen previously and then we can do
    // will move our element at index 4 and everything will shift forward

}